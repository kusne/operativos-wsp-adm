PASO 110 - WSP OPTIMIZACION CARGA + VENTANA UNICA

Base recibida: operativos-wsp-adm-main(7).zip
Fecha: 2026-06-14

Cambios aplicados sin modificar reglas funcionales de la app:
1) Carga inicial más rápida de operativos:
   - wsp-bootstrap.js inicia lectura REST de operativos_publicados apenas arranca, antes de cargar todos los módulos.
   - wsp.js reutiliza esa lectura pre-cargada si corresponde a la guardia vigente.
   - Se evita la segunda consulta simultánea solo para contar operativos durante el init.
   - La limpieza de Cache Storage / service workers queda diferida para no bloquear la pantalla inicial.

2) La ventana ya no se cierra cada X minutos:
   - wsp-whatsapp.js mantiene la apertura/compartir por WhatsApp, pero programarCierreVentanaWsp ya no cierra automáticamente la app.

3) Ventana única:
   - wsp-bootstrap.js agrega control de instancia única con Web Locks y fallback BroadcastChannel.
   - Si se abre una segunda ventana, intenta enfocar la existente y bloquea/cierra la duplicada cuando el navegador lo permite.
   - No usa localStorage.

No se tocaron: textos generados, validaciones, guardado Supabase de INICIO/FINALIZA/INFORMES, fotos, patentes, OCR, nomenclador ni reglas estadísticas.
